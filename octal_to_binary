#include <iostream>
#include<math.h>
using namespace std;

int main() {
	cout<<"GfG!";
	int n;
	cin>>n;
	string temp=to_string(n);
//	vector<int> v ;
	int j=0,ans=0;
    int k=temp.length();
	for(int i=k-1;i>=0;i--)
	{
	    ans+=(temp[i]-'0')*pow(8,j);
	    j++;
	}
	cout<<ans<<endl;
	return 0;
}
